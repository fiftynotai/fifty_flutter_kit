name: fifty_achievement_engine
description: Achievement system for Flutter games with condition-based unlocks, progress tracking, and FDL-compliant UI.
version: 0.1.0
repository: https://github.com/aspect-build/fifty_flutter_kit

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  fifty_tokens:
    path: ../fifty_tokens
  fifty_ui:
    path: ../fifty_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0
